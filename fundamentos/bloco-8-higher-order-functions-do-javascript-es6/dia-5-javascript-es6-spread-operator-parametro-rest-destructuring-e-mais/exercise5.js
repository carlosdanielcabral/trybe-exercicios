// Função que recebe um array de 3 elementos e retorno um novo array com o 1 e 3 elementos trocados, sendo que deve ser gasta apenas uma linha.

const { myList } = require('./data');

// el = elemento
const swap = ([ el1, el2, el3 ]) => [ el3, el2, el1 ];
